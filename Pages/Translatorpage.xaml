<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WindowsAISample.Pages.TranslatorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsAISample"
    xmlns:pages="using:WindowsAISample.Pages"
    xmlns:controls="using:WindowsAISample.Controls"
    xmlns:models="using:WindowsAISample.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalAlignment="Stretch">
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Stretch"
                    Margin="20">
            <TextBlock Text="Translator"
                       Style="{StaticResource TitleTextBlockStyle}"
                       Margin="10"/>
            <TextBlock Text="The Translator API allows you to translate text between multiple languages using Microsoft’s cloud-based neural machine translation technology."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Margin="10"/>
            <Border Margin="10"
                    CornerRadius="10"
                    BorderThickness="2"
                    BorderBrush="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefault}">
                <StackPanel Orientation="Vertical">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Vertical"  Margin="10" Grid.Row="1">
                            <ComboBox x:Name="LanguageBox"
                                      MinWidth="248"
                                      Margin="10"
                                      ItemsSource="{x:Bind languages}"
                                      Header="Target language:"
                                      SelectedIndex="0" />
                            <TextBox x:Name="InputTextBox"
                                     Margin="10"
                                     Height="150"
                                     AcceptsReturn="True"
                                     AutomationProperties.Name="Prompt input"
                                     Description=" "
                                     MaxLength="10000"
                                     PlaceholderText="Enter text to translate, language will be auto-detected..."
                                     TextChanged="InputBox_Changed"
                                     TextWrapping="Wrap" />
                        </StackPanel>  

                        <StackPanel Orientation="Horizontal"
                                    Margin="10"
                                    HorizontalAlignment="Right"
                                    Grid.Row="2">
                            <Button x:Name="TranslateButton"
                                    Margin="10"
                                    Content="Translate"
                                    Click="TranslateButton_Click"
                                    IsEnabled="False"
                                    Style="{ThemeResource AccentButtonStyle}" />
                            <Button x:Name="StopBtn"
                                    Margin="10"
                                    AutomationProperties.Name="Stop"
                                    Click="StopBtn_Click"
                                    Style="{ThemeResource AccentButtonStyle}"
                                    Visibility="Collapsed">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon x:Name="StopIcon"
                                              FontSize="16"
                                              Glyph="&#xE73B;" />
                                    <ProgressRing x:Name="OutputProgressBar"
                                                  Width="16"
                                                  Height="16"
                                                  Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                  IsIndeterminate="True"
                                                  Visibility="Collapsed" />
                                    <TextBlock Text="Stop" />
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <StackPanel Orientation="Vertical"  Margin="10" Grid.Row="3">
                            <TextBlock Text="Translation:"
                                       Margin="10"
                                       Style="{StaticResource BodyTextBlockStyle}"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="4"
                                      AutomationProperties.Name="Generated Text Scroll Area"
                                      HorizontalScrollBarVisibility="Disabled"
                                      IsTabStop="True"
                                      TabNavigation="Once"
                                      VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="TranslatedTextBlock"
                                       AutomationProperties.Name="Output"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       IsTextSelectionEnabled="True"
                                       TextWrapping="WrapWholeWords" />
                        </ScrollViewer>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
